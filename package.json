{
  "name": "airlink-controller-prototype",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "diagnose:network": "node scripts/network-diagnostics.js",
    "check:network-diagnostics": "node --check scripts/network-diagnostics.js"
  }
}
